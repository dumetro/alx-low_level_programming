#include <stdio.h>

/**
 * main - print the string in the put function
 * show output "with proper grammer, but the outcome is a piece of art"
 *
 * Return: Always 0 for success
 */

int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
